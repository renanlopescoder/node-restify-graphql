export default `
  type Product {
    _id: String,
    name: String,
    description: String,
  }

  type Query {
    allProducts: [Product!]!,
    findProduct(_id: String!): Product!
  }

  type Mutation {
    createProduct(name: String!, description: String!): Product!
  }
`;
